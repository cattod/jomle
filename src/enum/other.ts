export enum SAVE_PAGE_MODE {
    CREATE = 'CREATE',
    UPDATE = 'UPDATE',
}

/* export enum SAVE_PAGE_ACTION_BTN {
    REMOVE = 'REMOVE',
    CREATE = 'CREATE',
    UPDATE = 'UPDATE',
} */

export enum LIST_VIEW {
    GRID = 'GRID',
    CARD = 'CARD'
}
